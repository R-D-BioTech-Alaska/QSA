name: QSA Exact Hadamard Path Factor

on:
  pull_request:
    paths:
      - "include/qubit/qhpath_factor.hpp"
      - "tests/test_hpath_factor.cpp"
      - "benchmarks/hpath_factor.cpp"
      - ".github/workflows/qsa-hpath-factor.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hpath-factor-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Build Hadamard-path driver
        shell: bash
        run: |
          mkdir -p hpath-factor-driver
          cat > hpath-factor-driver/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.20)
          project(QSAExactHadamardPathCheck LANGUAGES CXX)
          set(CMAKE_CXX_STANDARD 20)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_CXX_EXTENSIONS OFF)
          set(QSTATE_BUILD_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_BENCHMARKS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_PYTHON_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_INSTALL_DEVELOPMENT OFF CACHE BOOL "" FORCE)
          add_subdirectory(.. qsa-core)
          enable_testing()
          add_executable(hpath_factor_tests ../tests/test_hpath_factor.cpp)
          target_link_libraries(hpath_factor_tests PRIVATE qstate_core)
          if(MSVC)
              target_compile_options(hpath_factor_tests PRIVATE /W4)
          else()
              target_compile_options(hpath_factor_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
          endif()
          add_test(NAME hpath_factor_tests COMMAND hpath_factor_tests)
          CMAKE
      - name: Configure
        run: cmake -S hpath-factor-driver -B hpath-factor-build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build hpath-factor-build --config Release --target hpath_factor_tests --parallel 2
      - name: Run
        run: ctest --test-dir hpath-factor-build -C Release --output-on-failure

  hpath-factor-sanitizers:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build sanitizer driver
        run: |
          mkdir -p hpath-factor-driver
          cat > hpath-factor-driver/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.20)
          project(QSAExactHadamardPathSanitize LANGUAGES CXX)
          set(CMAKE_CXX_STANDARD 20)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_CXX_EXTENSIONS OFF)
          set(QSTATE_BUILD_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_BENCHMARKS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_PYTHON_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_INSTALL_DEVELOPMENT OFF CACHE BOOL "" FORCE)
          add_compile_options(-fsanitize=address,undefined -fno-omit-frame-pointer)
          add_link_options(-fsanitize=address,undefined -fno-omit-frame-pointer)
          add_subdirectory(.. qsa-core)
          add_executable(hpath_factor_tests ../tests/test_hpath_factor.cpp)
          target_link_libraries(hpath_factor_tests PRIVATE qstate_core)
          CMAKE
          cmake -S hpath-factor-driver -B hpath-factor-sanitize-build -DCMAKE_BUILD_TYPE=Debug
          cmake --build hpath-factor-sanitize-build --target hpath_factor_tests --parallel 2
          ASAN_OPTIONS=detect_leaks=1 UBSAN_OPTIONS=print_stacktrace=1 ./hpath-factor-sanitize-build/hpath_factor_tests

  hpath-factor-evidence:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build evidence carrier
        run: |
          mkdir -p hpath-factor-driver
          cat > hpath-factor-driver/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.20)
          project(QSAExactHadamardPathEvidence LANGUAGES CXX)
          set(CMAKE_CXX_STANDARD 20)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_CXX_EXTENSIONS OFF)
          set(QSTATE_BUILD_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_BENCHMARKS OFF CACHE BOOL "" FORCE)
          set(QSTATE_BUILD_PYTHON_TESTS OFF CACHE BOOL "" FORCE)
          set(QSTATE_INSTALL_DEVELOPMENT OFF CACHE BOOL "" FORCE)
          add_subdirectory(.. qsa-core)
          add_executable(hpath_factor_benchmark ../benchmarks/hpath_factor.cpp)
          target_link_libraries(hpath_factor_benchmark PRIVATE qstate_core)
          CMAKE
          cmake -S hpath-factor-driver -B hpath-factor-evidence-build -DCMAKE_BUILD_TYPE=Release
          cmake --build hpath-factor-evidence-build --target hpath_factor_benchmark --parallel 2
      - name: Run Hadamard-path carriers
        run: |
          set -o pipefail
          ./hpath-factor-evidence-build/hpath_factor_benchmark | tee hpath-factor-evidence.txt
      - name: Enforce bounded evidence
        run: |
          python - <<'PY'
          import math
          values = {}
          with open("hpath-factor-evidence.txt", "r", encoding="utf-8") as handle:
              for line in handle:
                  key, sep, value = line.strip().partition("=")
                  if sep:
                      values[key] = value
          assert int(values["spatial_qubits"]) == 1536
          assert int(values["spatial_h_events"]) == 1536
          assert int(values["spatial_raw_branch_count_log2"]) == 1536
          assert int(values["spatial_factor_variables"]) == 1536
          assert int(values["spatial_factor_count"]) == 3071
          assert int(values["spatial_peak_union_variables"]) <= 2
          assert int(values["spatial_peak_factor_entries"]) <= 4
          assert int(values["spatial_workspace_slots"]) <= 4
          assert float(values["spatial_dense_to_peak_factor_ratio_log2"]) > 1533.0
          assert abs(float(values["spatial_log2_probability_zero"]) + 1536.0) < 1e-9
          assert 0.0 < float(values["spatial_compile_us"]) < 1_000_000.0
          assert 0.0 < float(values["spatial_query_ms"]) < 10000.0
          assert int(values["spacetime_qubits"]) == 1024
          assert int(values["spacetime_rounds"]) == 8
          assert int(values["spacetime_h_events"]) == 8192
          assert int(values["spacetime_raw_branch_count_log2"]) == 8192
          assert int(values["spacetime_h_active_qubits"]) == 1024
          assert int(values["spacetime_factor_variables"]) == 8192
          assert int(values["spacetime_peak_union_variables"]) <= 12
          assert int(values["spacetime_peak_factor_entries"]) <= 4096
          assert 0 < int(values["spacetime_workspace_slots"]) <= 10000
          assert float(values["spacetime_path_to_peak_factor_ratio_log2"]) > 8180.0
          assert float(values["spacetime_dense_to_peak_factor_ratio_log2"]) > 1012.0
          spacetime_logp = float(values["spacetime_log2_probability_probe"])
          assert math.isfinite(spacetime_logp) and spacetime_logp <= 1e-9
          assert 0 < int(values["spacetime_metadata_estimated_bytes"]) < 64 * 1024 * 1024
          assert 0 < int(values["spacetime_graph_estimated_bytes"]) < 128 * 1024 * 1024
          assert 0 < int(values["spacetime_plan_estimated_bytes"]) < 512 * 1024 * 1024
          assert 0.0 < float(values["spacetime_compile_us"]) < 1_000_000.0
          assert 0.0 < float(values["spacetime_query_ms"]) < 30000.0
          assert int(values["spacetime_physical_dense_log2_amplitudes"]) == 1024
          assert int(values["spacetime_contains_arbitrary_rz"]) == 1
          assert int(values["hard_qubits"]) == 32
          assert int(values["hard_cz_edges"]) == 496
          assert int(values["hard_factor_entry_cap"]) == 1024
          assert int(values["hard_dense_width_rejected"]) == 1
          assert 0.0 <= float(values["hard_rejection_ms"]) < 1000.0
          assert int(values["dense_state_materialized"]) == 0
          assert int(values["explicit_h_branch_list_materialized"]) == 0
          PY
      - name: Hash evidence
        run: sha256sum hpath-factor-evidence.txt | tee hpath-factor-evidence.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: qsa-hpath-factor-evidence
          path: |
            hpath-factor-evidence.txt
            hpath-factor-evidence.sha256
