name: QSA Exact Tropical QTT

on:
  pull_request:
    paths:
      - "include/qubit/qtropical_qtt.hpp"
      - "tests/test_exact_tropical_qtt.cpp"
      - "benchmarks/tropical_qtt.cpp"
      - "tests/test_architecture_layers.py"
      - ".github/workflows/qsa-tropical-qtt.yml"
      - ".github/workflows/qsa-tropical-qtt-evidence.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tropical-qtt-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-latest, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Create standalone Tropical QTT build
        shell: bash
        run: |
          mkdir -p tropical-qtt-driver
          cat > tropical-qtt-driver/CMakeLists.txt <<'EOF'
          cmake_minimum_required(VERSION 3.20)
          project(QSAExactTropicalQTTCheck LANGUAGES CXX)
          set(CMAKE_CXX_STANDARD 20)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_CXX_EXTENSIONS OFF)
          enable_testing()
          add_executable(tropical_qtt_tests ../tests/test_exact_tropical_qtt.cpp)
          target_include_directories(tropical_qtt_tests PRIVATE ../include)
          if(MSVC)
              target_compile_options(tropical_qtt_tests PRIVATE /W4)
          else()
              target_compile_options(tropical_qtt_tests PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
          endif()
          add_test(NAME tropical_qtt_tests COMMAND tropical_qtt_tests)
          EOF
      - name: Configure
        run: cmake -S tropical-qtt-driver -B tropical-qtt-build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build tropical-qtt-build --config Release --parallel 2
      - name: Run
        run: ctest --test-dir tropical-qtt-build -C Release --output-on-failure

  tropical-qtt-sanitizers:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build and run ASan/UBSan
        run: |
          c++ -std=c++20 -O1 -g -fsanitize=address,undefined -fno-omit-frame-pointer \
            -Wall -Wextra -Wpedantic -Wconversion -Wshadow -Iinclude \
            tests/test_exact_tropical_qtt.cpp -o exact-tropical-qtt-sanitize
          ASAN_OPTIONS=detect_leaks=1 UBSAN_OPTIONS=print_stacktrace=1 ./exact-tropical-qtt-sanitize
