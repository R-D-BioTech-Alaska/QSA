# Hardening scope

This change set is limited to correctness and review infrastructure around the existing QSA 0.1.8 runtime.

It adds transition differential coverage, copy-on-write isolation checks, architecture dependency gates, and operation-derived Clifford certification. Existing runtime algorithms, QSC format, C ABI, and public compatibility contracts remain unchanged.
